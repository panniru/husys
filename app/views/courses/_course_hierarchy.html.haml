.tree
  %ul
    - data.each do |course|
      %li
        %span
          %i.glyphicon.glyphicon-plus-sign.pointer{:data=> {:view => 'sub_category', :filters => "#{course.category}" }, :onclick => 'CourseDrillHandler.getInstance().performDrill(event)'}
          = course.category
