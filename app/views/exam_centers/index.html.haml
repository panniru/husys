.page-header
  %h2
    = "Exam Centers"
    .pull-right
      = link_to "Add Center", new_exam_center_path, :class => "btn btn-primary"
.alert.alert-info
  = page_entries_info @centers, model: "results", html: false

= render "shared/gmap"
%br
= render 'results'

= will_paginate @exam_centers, param_name: "exam_center_page", previous_label: h("<"), next_label: h(">")
